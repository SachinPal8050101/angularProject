import { CalculateCurrencyPipe } from './calculate-currency.pipe';

describe('CalculateCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
